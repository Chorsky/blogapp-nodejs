<h4>Categorias: </h4>

{{#each categorias}}
    <div class="card mt-4">
        <div class="card-body">
            <a href="/categorias/{{slug}}"><h5>{{nome}}</h5></a>
        </div>
    </div>
{{else}}    
    <h4 class="mt-4">Nenhuma categoria registrada</h4>
{{/each}}